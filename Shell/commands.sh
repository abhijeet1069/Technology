#!/bin/sh


# Home directory
	~/ : ~ means home directory of the logged in user and / is beginning of a directory

# > and >> operator
	> overwrites an existing file else creates new file
	>> appends an existing file else creates new file.
